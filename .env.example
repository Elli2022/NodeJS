# Local API (filesystem — default for tests and dev)
NODE_HOSTNAME=127.0.0.1
NODE_PORT=3000
NODE_NAME=nodejs-ms
STORAGE=file

# Production (Render + Neon): set STORAGE=postgres and paste Neon connection string
# STORAGE=postgres
# DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
